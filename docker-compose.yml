version: "3.9"
services:
  resume:
    image: blang/latex:latest
    container_name: ethan605_make_resume
    environment:
      - VARIANT
    command: xelatex "$VARIANT.tex"
    volumes:
      - ./resumes:/data
