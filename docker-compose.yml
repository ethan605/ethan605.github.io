version: "3.9"
services:
  resume:
    image: ethan605/texlive:full
    build: .
    container_name: ethan605_resume
    environment:
      - ENTRYPOINT
    command: xelatex "$ENTRYPOINT"
    volumes:
      - ./resumes:/app/data
