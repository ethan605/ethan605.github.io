version: "3.8"
services:
  resume:
    image: blang/latex:latest
    container_name: ethan605_make_resume
    environment:
      - VARIANT
    command: pdflatex "$VARIANT.tex"
    volumes:
      - ./resumes:/data
